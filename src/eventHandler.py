KEYS = {
		"w": ,
		"s",
		"a",
		"d"
		}


import pygame as pg
def createEvent(ev):
	if ev.type == pg.KEYDOWN:
		
class Event:
	def __init__(self, ev):
		self.ev = ev
		self.type = event.type
		self.dict = event.dict
		
		self.player = player
	
		if self.type